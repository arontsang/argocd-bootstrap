apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: http-proxy
resources:
  - http.yaml
  - namespace.yaml
  - proxy.yaml
  - service.yaml
  - socat-proxy.yaml
configMapGenerator:
  - name: script
    files: 
      - wpad.js
  - name: socat-scripts
    files:
      - socat.sh