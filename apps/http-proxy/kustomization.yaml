apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: http-proxy
resources:
  - namespace.yaml
  - smart-proxy.yaml
  - uk-proxy.yaml
  - wpad.yaml
  - rollout-restart-cronjob.yaml
configMapGenerator:
  - name: script
    files: 
      - wpad.js
